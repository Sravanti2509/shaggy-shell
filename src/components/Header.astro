<header class="header">

  <div class="navbar">

    <!-- BRAND -->
    <a href="/" class="brand">

      <div class="logo-box">
        <img
          src="/products/logo.jpg"
          alt="Sravs Bakery"
        />
      </div>

      <div class="brand-text">
        <span class="brand-name">Sravs Bakery</span>
        <span class="brand-tagline">FRESHLY BAKED</span>
      </div>

    </a>


    <!-- NAVIGATION -->
    <nav>

      <a href="/" class="nav-link">
        Home
      </a>

      <a href="/menu" class="nav-link">
        Menu
      </a>

      <a href="/about" class="nav-link">
        About
      </a>

      <a href="/contact" class="nav-link">
        Contact
      </a>


      <!-- CHECKOUT -->
      <a href="/checkout" class="cart-link">

        <span>Checkout</span>

        <span class="cart-wrapper">

          <svg
            class="cart-icon"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M3 4H5L7.2 14.2C7.31 14.67 7.72 15 8.2 15H17.8C18.28 15 18.69 14.67 18.8 14.2L20.5 7H6"
              stroke="currentColor"
              stroke-width="1.8"
              stroke-linecap="round"
              stroke-linejoin="round"
            />

            <circle
              cx="9"
              cy="19"
              r="1.4"
              fill="currentColor"
            />

            <circle
              cx="17"
              cy="19"
              r="1.4"
              fill="currentColor"
            />
          </svg>


          <span
            id="cart-count"
            class="cart-count"
          >
            0
          </span>

        </span>

      </a>

    </nav>

  </div>

</header>


<script>
  function updateCartCount() {

    const cart = JSON.parse(
      localStorage.getItem("cart") || "[]"
    );

    const count = cart.reduce(
      (total, item) => {
        return total + (
          Number(item.quantity) || 1
        );
      },
      0
    );

    const cartCount =
      document.getElementById("cart-count");

    if (cartCount) {
      cartCount.textContent = count;
    }
  }


  updateCartCount();


  window.addEventListener(
    "storage",
    updateCartCount
  );


  window.addEventListener(
    "focus",
    updateCartCount
  );
</script>


<style>

  /* ===============================
     HEADER
  =============================== */

  .header {
    width: 100%;

    background: #ffffff;

    border-bottom:
      1px solid #e8edf3;

    position: relative;

    z-index: 100;

    box-shadow:
      0 3px 18px rgba(15, 35, 60, 0.04);
  }


  /* ===============================
     NAVBAR
  =============================== */

  .navbar {
    max-width: 1250px;

    min-height: 88px;

    margin: 0 auto;

    padding: 8px 38px;

    display: flex;

    align-items: center;

    justify-content: space-between;

    box-sizing: border-box;
  }


  /* ===============================
     BRAND
  =============================== */

  .brand {
    display: flex;

    align-items: center;

    gap: 12px;

    text-decoration: none;

    flex-shrink: 0;
  }


  .logo-box {
    width: 68px;

    height: 68px;

    display: flex;

    align-items: center;

    justify-content: center;

    flex-shrink: 0;
  }


  .logo-box img {
    width: 100%;

    height: 100%;

    object-fit: contain;

    display: block;
  }


  /* ===============================
     BRAND TEXT
  =============================== */

  .brand-text {
    display: flex;

    flex-direction: column;

    justify-content: center;
  }


  .brand-name {
    font-family:
      Georgia,
      "Times New Roman",
      serif;

    font-size: 25px;

    font-weight: 700;

    color: #10233f;

    letter-spacing: 0.2px;

    white-space: nowrap;
  }


  .brand-tagline {
    margin-top: 5px;

    font-family: Arial, sans-serif;

    font-size: 8px;

    font-weight: 700;

    color: #416b94;

    letter-spacing: 3px;
  }


  /* ===============================
     NAVIGATION
  =============================== */

  nav {
    display: flex;

    align-items: center;

    gap: 34px;
  }


  .nav-link {
    position: relative;

    padding: 8px 0;

    color: #27394f;

    text-decoration: none;

    font-family:
      Arial,
      sans-serif;

    font-size: 15px;

    font-weight: 500;

    letter-spacing: 0.2px;

    transition:
      color 0.25s ease;
  }


  .nav-link::after {
    content: "";

    position: absolute;

    left: 0;

    bottom: 0;

    width: 0;

    height: 2px;

    background: #416b94;

    border-radius: 2px;

    transition:
      width 0.25s ease;
  }


  .nav-link:hover {
    color: #315d88;
  }


  .nav-link:hover::after {
    width: 100%;
  }


  /* ===============================
     CHECKOUT BUTTON
  =============================== */

  .cart-link {
    display: flex;

    align-items: center;

    gap: 9px;

    padding:
      10px 16px;

    border-radius: 8px;

    background: #10233f;

    color: white;

    text-decoration: none;

    font-family:
      Arial,
      sans-serif;

    font-size: 14px;

    font-weight: 600;

    letter-spacing: 0.2px;

    box-shadow:
      0 5px 14px
      rgba(16, 35, 63, 0.16);

    transition:
      background 0.25s ease,
      transform 0.25s ease,
      box-shadow 0.25s ease;
  }


  .cart-link:hover {
    background: #315d88;

    transform:
      translateY(-1px);

    box-shadow:
      0 7px 18px
      rgba(16, 35, 63, 0.2);
  }


  /* ===============================
     CART ICON
  =============================== */

  .cart-wrapper {
    position: relative;

    width: 22px;

    height: 22px;

    display: flex;

    align-items: center;

    justify-content: center;
  }


  .cart-icon {
    width: 21px;

    height: 21px;

    color: white;

    display: block;
  }


  /* ===============================
     CART COUNT
  =============================== */

  .cart-count {
    position: absolute;

    top: -8px;

    right: -9px;

    min-width: 17px;

    height: 17px;

    padding: 0 4px;

    display: flex;

    align-items: center;

    justify-content: center;

    box-sizing: border-box;

    border-radius: 50%;

    background: #d95c5c;

    color: white;

    border:
      2px solid #10233f;

    font-family: Arial, sans-serif;

    font-size: 9px;

    font-weight: 700;

    line-height: 1;
  }


  /* ===============================
     TABLET
  =============================== */

  @media (max-width: 850px) {

    .navbar {
      padding:
        8px 20px;
    }

    nav {
      gap: 20px;
    }

    .brand-name {
      font-size: 22px;
    }
  }


  /* ===============================
     MOBILE
  =============================== */

  @media (max-width: 650px) {

    .navbar {
      min-height: 68px;

      padding:
        7px 10px;

      gap: 8px;
    }


    .logo-box {
      width: 45px;

      height: 45px;
    }


    .brand {
      gap: 7px;
    }


    .brand-name {
      font-size: 16px;
    }


    .brand-tagline {
      font-size: 5px;

      letter-spacing: 2px;

      margin-top: 3px;
    }


    nav {
      gap: 10px;

      min-width: 0;

      justify-content: flex-end;
    }


    .nav-link {
      font-size: 10px;

      padding: 5px 0;
    }


    .cart-link {
      padding:
        8px;

      border-radius: 7px;
    }


    .cart-link > span:first-child {
      display: none;
    }


    .cart-wrapper {
      width: 21px;

      height: 21px;
    }
  }


  /* ===============================
     VERY SMALL PHONE
  =============================== */

  @media (max-width: 390px) {

    .navbar {
      padding:
        6px 7px;
    }


    .logo-box {
      width: 40px;

      height: 40px;
    }


    .brand-name {
      font-size: 14px;
    }


    .brand-tagline {
      font-size: 4px;

      letter-spacing: 1.5px;
    }


    nav {
      gap: 7px;
    }


    .nav-link {
      font-size: 9px;
    }


    .cart-link {
      padding: 7px;
    }


    .cart-icon {
      width: 19px;

      height: 19px;
    }
  }

</style>